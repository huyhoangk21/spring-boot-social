spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/instagram
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
  security:
    user:
      name: root
      password: root

server:
  port: 9090
  error:
    include-message: always

